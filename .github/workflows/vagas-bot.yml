name: Bot Vagas TI

on:
  schedule:
    # ⏰ HORÁRIOS AGENDADOS (UTC)
    # 11h UTC = 8h Brasília | 14h UTC = 11h Brasília | 17h UTC = 14h Brasília | 20h UTC = 17h Brasília | 23h UTC = 20h Brasília
    - cron: '0 11,14,17,20,23 * * *'
  workflow_dispatch:  # Permite executar manualmente

jobs:
  buscar-vagas:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
    - name: 📥 Checkout do código
      uses: actions/checkout@v4
      
    - name: 🐍 Configurar Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
        
    - name: 🌐 Instalar Chromium
      run: |
        sudo apt-get update
        sudo apt-get install -y chromium-browser
        sudo apt-get install -y libxss1 fonts-liberation xdg-utils
        
    - name: ✅ Verificar Chromium
      run: chromium-browser --version
        
    - name: 📦 Instalar dependências Python
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: 🚀 Executar Bot de Vagas
      env:
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        LOCAL: "Salvador, BA"
        PYTHONUNBUFFERED: 1
      run: python bot_vagas.py
        
    - name: ✅ Sucesso
      if: success()
      run: echo "✅ Bot executado com sucesso!"
        
    - name: ❌ Erro no Discord
      if: failure()
      run: |
        curl -X POST ${{ secrets.DISCORD_WEBHOOK_URL }} \
          -H 'Content-Type: application/json' \
          -d '{"content":"❌ Erro ao executar bot. Verifique os logs."}' || true
